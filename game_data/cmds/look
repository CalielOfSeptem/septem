inherit command
c = command.new()
c:SetVerb("look")

function command:execute_command ( o, ... )
	print( 'Type of o = ' .. type(o) )
	p = player_cast(o)
	if p ~= nil then
		p:SendToEntity('HELLO FROM COMMAND')
	end

	printResult = ""
	for i,v in ipairs(arg) do
		printResult = printResult .. tostring(v) .. "\t"
	end
	printResult = printResult .. "\n"
end
